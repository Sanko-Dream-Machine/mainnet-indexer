{
  "name": "blocks",
  "license": "MIT-0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ stanko",
    "create-local": "graph create --node http://localhost:8020/ stanko",
    "remove-local": "graph remove --node http://localhost:8020/ stanko",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 stanko",
    "test": "graph test"
  },
  "dependencies": {
    "@babel/preset-typescript": "^7.23.3",
    "@graphprotocol/graph-cli": "^0.68.0",
    "@graphprotocol/graph-ts": "^0.33.0"
  },
  "devDependencies": {
    "matchstick-as": "^0.6.0"
  }
}
